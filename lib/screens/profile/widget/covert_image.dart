import 'package:flutter/material.dart';
import 'package:rayek_v001/utils/palette.dart';

class CouvertImage extends StatelessWidget {
  const CouvertImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
